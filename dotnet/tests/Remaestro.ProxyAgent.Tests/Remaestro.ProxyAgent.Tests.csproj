<Project Sdk="Microsoft.NET.Sdk">

  <PropertyGroup>
    <TargetFramework>net10.0</TargetFramework>
    <IsPackable>false</IsPackable>
  </PropertyGroup>

  <ItemGroup>
    <PackageReference Include="Microsoft.NET.Test.Sdk" Version="17.11.1" />
    <PackageReference Include="xunit" Version="2.9.2" />
    <PackageReference Include="xunit.runner.visualstudio" Version="2.8.2" />
  </ItemGroup>

  <ItemGroup>
    <Using Include="Xunit" />
  </ItemGroup>

  <!--
    One project reference, and that is the whole point. The hub side of the protocol is written out as
    literals in HubWire.cs rather than linked, so these tests are a conformance suite anyone can run against
    a proxy in any language — see the note at the top of that file.
  -->
  <ItemGroup>
    <ProjectReference Include="..\..\src\Remaestro.ProxyAgent\Remaestro.ProxyAgent.csproj" />
  </ItemGroup>

</Project>
